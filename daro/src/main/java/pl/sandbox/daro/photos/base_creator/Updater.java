package pl.sandbox.daro.photos.base_creator;


public class Updater {

  public String printUpdated() {
    //TODO: Create a method which scans whole folder for files which have been created using builderFile method
    // if there is one do nothing, if more than one then compare newest file with old one. If files matches do nothing
    // else print updated or new records.
    return "";
  }
}
